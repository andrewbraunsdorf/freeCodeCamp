	// lendBook: function () {
	// 	if (this.books.length > 0 ) {
	// 		var randomBook = this.books[Math.floor(Math.random() * this.books.length)];
	// 		this.lentBooks.unshift(this.books.splice(this.books.indexOf(randomBook), 1)[0]);
	// 	}
	// },

    /*	lendBook: function () {
		if (this.books.length > 0 ) {
			var randomBook = this.books[Math.floor(Math.random() * this.books.length)];
			this.lentBooks.unshift(this.books.splice(this.books.indexOf(randomBook), 1)[0]);
		}
        }, */
